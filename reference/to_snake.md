# Convert camel case to snake case

Convert camel case to snake case

## Usage

``` r
to_snake(name)
```

## Arguments

- name:

  a character vector

## Value

same length as `name` but with snake case

## Examples

``` r
to_snake("sparseDataRange")
#> [1] "sparse_data_range"
```
